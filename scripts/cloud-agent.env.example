# Copy to scripts/cloud-agent.env.local (gitignored via .env.*)
# Or set CURSOR_API_KEY in repo .env / castalia.institute .env (cloud-agent.sh loads those too).
# GitHub Actions: ./scripts/sync-cursor-github-secret.sh  (once)
# Create key: https://cursor.com/dashboard/integrations
CURSOR_API_KEY=
