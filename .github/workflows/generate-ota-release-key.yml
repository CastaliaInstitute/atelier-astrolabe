name: Generate OTA Release Key

on:
  workflow_dispatch: {}

permissions:
  contents: read

jobs:
  generate:
    name: Generate encrypted OTA signing key
    runs-on: ubuntu-latest
    steps:
      - name: Check runner
        run: echo "OTA release key workflow is available"
